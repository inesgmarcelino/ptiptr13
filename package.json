{
  "name": "ecomarket-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "install": "cd ecomarket-backend && npm install && cd .. && cd ecomarket-frontend && npm install",
    "backend": "node ./ecomarket-backend/bin/www",
    "frontend": "cd ecomarket-frontend && npm start ",
    "test": "npm run backend && npm run frontend",
    "start": "NODE_ENV=production node ./ecomarket-backend/bin/www && cd ecomarket-frontend && npm run build && serve -s build",
    "deploy": "npm install && npm start"
  }
}
