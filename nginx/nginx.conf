events {
    worker_connections 1024;
}
http {

    server {
        listen 80;

        location /api/ {
            proxy_pass backend
        }

        location / {
            proxy_pass frontend;
        }
    }
}